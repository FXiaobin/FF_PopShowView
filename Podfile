# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'FF_PopShowView' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  
  pod 'Masonry'

  # Pods for FF_PopShowView

  target 'FF_PopShowViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FF_PopShowViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
